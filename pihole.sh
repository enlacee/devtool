## actualizar pihole
pihole -up

# actualizar la lista de listadePaginas bloqueadas
pihole -g

  [✓] DNS service is listening
     [✓] UDP (IPv4)
     [✓] TCP (IPv4)
     [✓] UDP (IPv6)
     [✓] TCP (IPv6)
# bloquear una pagina por terminal
pihole -b advertiser.example.comg

# ver los log de todo el trafico en la red
pihole tail

################################
## configurar ipv6 en RASPBERRY: 
## para bloquear trafico total en WINDOWS 10 : es cliente ipv6 por defecto
###############################
* Configurar el router el soporte RED LAN IPV6
* Configurar activar ipv6 en el servidor (RASPBERRY)
